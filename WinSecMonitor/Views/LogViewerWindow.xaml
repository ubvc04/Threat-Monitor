<Window x:Class="WinSecMonitor.Views.LogViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Log Viewer" Height="600" Width="900" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Log Viewer" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
        
        <!-- Filters -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Filter:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Grid.Column="1" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" 
                     VerticalAlignment="Center" Margin="0,0,10,0"/>
            
            <TextBlock Grid.Column="2" Text="Level:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox Grid.Column="3" ItemsSource="{Binding LogLevels}" 
                      SelectedItem="{Binding SelectedLogLevel}" 
                      Width="100" Margin="0,0,10,0"/>
            
            <TextBlock Grid.Column="4" Text="Date:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker Grid.Column="5" SelectedDate="{Binding SelectedDate}" Width="120" Margin="0,0,10,0"/>
            
            <Button Grid.Column="6" Content="Refresh" Command="{Binding RefreshCommand}" 
                    Padding="10,3" Margin="0,0,0,0"/>
        </Grid>
        
        <!-- Log Entries -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding FilteredLogEntries}" 
                  AutoGenerateColumns="False" IsReadOnly="True"
                  AlternatingRowBackground="#F5F5F5"
                  CanUserSortColumns="True"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp}" Width="150"/>
                <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="80">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Level}" Value="Critical">
                                    <Setter Property="Foreground" Value="DarkRed"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Level}" Value="Error">
                                    <Setter Property="Foreground" Value="Red"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Level}" Value="Warning">
                                    <Setter Property="Foreground" Value="Orange"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Level}" Value="Information">
                                    <Setter Property="Foreground" Value="Blue"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Level}" Value="Debug">
                                    <Setter Property="Foreground" Value="Gray"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <!-- Footer -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Button Content="Export" Command="{Binding ExportCommand}" Padding="10,3" Margin="0,0,10,0"/>
            <Button Content="Clear Logs" Command="{Binding ClearLogsCommand}" Padding="10,3" Margin="0,0,10,0"/>
            <Button Content="Close" IsCancel="True" Padding="10,3"/>
        </StackPanel>
    </Grid>
</Window>